# Database Configuration
# Format: mysql://[username]:[password]@[host]:[port]/[database]
DATABASE_URL="mysql://user:password@localhost:3306/pm_database"

# Authentication Configuration
# Generate a new secure secret key using: openssl rand -base64 32
# Example (DO NOT USE THIS IN PRODUCTION):
JWT_SECRET="base64_encoded_secret_key_min_32_chars_length"
JWT_EXPIRES_IN="7d"
BCRYPT_SALT_ROUNDS="10"

# Role Configuration
DEFAULT_ROLE="Employee"

# Test Configuration (optional)
# Replace these with secure values for testing
TEST_ADMIN_EMAIL="admin@example.com"
TEST_ADMIN_PASS="change_this_to_secure_password"
TEST_USER_EMAIL="user@example.com"
TEST_USER_PASS="change_this_to_secure_password"

# Security Notes:
# 1. Never commit the actual .env file
# 2. Use strong passwords in production
# 3. Generate a unique JWT_SECRET for each environment
# 4. Keep your .env file secure and never share it
